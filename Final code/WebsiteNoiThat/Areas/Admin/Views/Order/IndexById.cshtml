@*@model  IEnumerable<WebsiteNoiThat.Models.OrderViewModel>*@
@using WebsiteNoiThat.Areas.Admin.Models
@model Models.EF.Order
@{
    ViewBag.Title = "IndexById";
}

<h3 style="text-align:center;">
    Wooden Furniture<br />
    Address: Sài Gòn<br />
    Phone Number : 0833669999<br />
    Email: dragonemxike@gmail.com
</h3>
<hr />
<h4 style="font-size:16px; text-align:left;">
        Invoice ID : @Model.OrderId<br />
        Name  : @Model.ShipName<br />
        Phone numberi: @Model.ShipPhone<br />
        Address: @Model.ShipAddress<br />
        Update Date : @Model.UpdateDate<br />
    </h4>
<h3 style="text-align:center;">List Of Products</h3>
<hr />
<table id="example" class="table table-striped table-bordered table-hover" style="width:100%; text-align:center;">
    <thead>
        <tr>
            <th scope="col">Product ID</th>
            <th scope="col">Product Name</th>
            <th scope="col">Quantity</th>
            <th scope="col">Price (Promotion If available)</th>
        </tr>
        @*<tr>
            <th>Mã hóa đơn</th>
            <th>Mã sản phẩm</th>
            <th>Giá</th>
            <th>Số lượng</th>
            <th>Giảm giá</th>
            <th>Tên khách hàng</th>
            <th>Địa chỉ</th>
            <th>Số điện thoại</th>
            <th>Ngày cập nhật</th>
            <th>Thành tiền</th>
            <th>Xác nhận thanh toán</th>
        </tr>*@
    </thead>

    <tbody>
        <tr>
            @foreach (OrderProduct p in (List<OrderProduct>)ViewBag.orderproducts)
            {
            <tr>
                <td scope="row">@p.ProductId</td>
                <td>@p.ProductName</td>
                <td>@p.Quantity</td>
                <td>@p.Price₫</td>
            </tr>
        }
            <tr>
                @*@foreach (var item in Model)
                {
                    <tr>

                        <td> @item.OrderId</td>
                        <td>@item.ProductId</td>

                        <td>@item.Price.GetValueOrDefault(0).ToString("N0")</td>
                        <td>@item.Quantity</td>
                        <td>@item.Discount</td>
                        <td>@item.ShipName</td>
                        <td>@item.ShipAddress</td>
                        <td>@item.ShipPhone</td>
                        <td>@item.UpdateDate</td>
                        <td>
                            @(((item.Quantity * item.Price) - (item.Quantity * item.Price * item.Discount * 0.01)).GetValueOrDefault(0).ToString("N0"))
                        </td>
                        <td></td>




                    </tr>
                }*@

        </tbody>
    <tfoot>

        @* <tr><td colspan="9">Tổng tiền thanh toán: </td><td> @string.Format("{0:#,###.##}", )</td></tr>*@
    </tfoot>
</table>
<hr />
<a style="padding-right:0;">Total: <span style="font-weight:bold; color:crimson;">  @ViewBag.total.ToString("N0") ₫</span></a>




